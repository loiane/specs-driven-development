---
name: shipping-and-launch
description: "Pre-deploy hygiene for a Spring Boot 4 feature — verify gates, capture rollback plan, sign off observability, generate release notes, stage the rollout. Used by /ship."
---

# shipping-and-launch (Antigravity wrapper)

This skill is defined in `shared/skills/shipping-and-launch/SKILL.md`. Read that file and follow it verbatim. Do not duplicate the content here — keep the shared file as the single source of truth so all editors stay in sync.
