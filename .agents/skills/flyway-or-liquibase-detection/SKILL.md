---
name: flyway-or-liquibase-detection
description: "Auto-detect the project's DB migration tool and follow its conventions. Never use both. Use when designing or writing a schema change."
---

# flyway-or-liquibase-detection (Antigravity wrapper)

This skill is defined in `shared/skills/flyway-or-liquibase-detection/SKILL.md`. Read that file and follow it verbatim. Do not duplicate the content here — keep the shared file as the single source of truth so all editors stay in sync.
