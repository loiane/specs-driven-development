---
name: openapi-contract-first
description: "Contract-first OpenAPI workflow — author or update the spec, run the diff gate, regenerate types, write the controller. Use when adding or changing an HTTP endpoint."
---

# openapi-contract-first (Antigravity wrapper)

This skill is defined in `shared/skills/openapi-contract-first/SKILL.md`. Read that file and follow it verbatim. Do not duplicate the content here — keep the shared file as the single source of truth so all editors stay in sync.
