---
name: adr-authoring
description: "Author MADR-style Architecture Decision Records under `.specs/<feature-id>/adr/`. Use whenever a non-obvious technical decision is made, a default is overridden, or a waiver is granted."
---

# adr-authoring (Antigravity wrapper)

This skill is defined in `shared/skills/adr-authoring/SKILL.md`. Read that file and follow it verbatim. Do not duplicate the content here — keep the shared file as the single source of truth so all editors stay in sync.
