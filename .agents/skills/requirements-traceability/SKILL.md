---
name: requirements-traceability
description: "Build and verify the AC to tasks to tests to code symbols to gates traceability matrix in 07a-traceability.md. Use when validating no AC is uncovered."
---

# requirements-traceability (Antigravity wrapper)

This skill is defined in `shared/skills/requirements-traceability/SKILL.md`. Read that file and follow it verbatim. Do not duplicate the content here — keep the shared file as the single source of truth so all editors stay in sync.
